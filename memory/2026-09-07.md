# 2026-09-07

## Sanctuary homepage completed (design brief: boutique digital sanctuary)
- Resumed the uncommitted redesign from a previous session (global.css + Layout.astro were already done: parchment/olive/terracotta tokens, Cormorant Garamond/EB Garamond/Noto Serif SC, paper-grain veil, 800ms reveal, calm hover).
- Built out index.astro: hero with logo + sanctuary name + verse-of-the-day (3 crossfading slides resolved from the DAY's lectionary readings: focus gospel → epistle → first morning psalm; graceful skip when the bundled 182-chapter store lacks a slot; fixed Matt 11:28-30 fallback), 6s breathing light cycle, Begin Reading → /today.
- Four Movements cards → /approach#lectio|meditatio|oratio|contemplatio (anchors added to approach.astro dt-cards + scroll-margin).
- Receive-a-Word section wraps the EXISTING draw flow unchanged (form/results/gates intact); revealed results auto-scroll into view.
- Guide-reflection keeper card (olive-branch SVG, 2 hover annotations, bilingual, honest "keeper" signature — no fake person).
- Seasons of Reading: 6 plans (one per liturgical season) computed from lectionaryDays.json season runs; in-season plan shows "Day N of M" + progress bar (real church-calendar progress, no fake user data).
- Daily Invitation: email capture wired to /api/daily-invitation (POST JSON → D1 daily_invitations table; duplicate = success).
- Ambient bell: opt-in toggle on silencio; Web Audio synth (432Hz bowl partials, 5.5s decay); localStorage lectio_bell; first user gesture triggers chime (autoplay-safe).
- Verified: astro check 0 errors, 314/314 vitest, build OK, live preview render + judge visual gate PASS on home en/zh, silencio, pricing, approach, login.

## This machine's local runtime (macOS, new environment)
- Old-machine workarounds don't apply. Findings: `astro dev` hangs at vite re-optimizing; `wrangler dev` hangs after config parse (no listener, no error); `astro preview` needs REMOTE bindings auth → FAILS unless `wrangler login` done (browser OAuth).
- **After `npx wrangler login` (OAuth, joechenst@gmail.com, account 394272e5848251ac42a2d4535dfeab36), `astro preview` WORKS.** This is the local-run path now. Miniflare 5-alpha API is a rewrite (narrow `config` object) — do NOT hand-roll adapters.
- .dev.vars created locally (SESSION_SECRET random + OPENAI_API_KEY placeholder — placeholder makes module load; real AI reflections need the real key in .env/.dev.vars).
- GOTCHA: `.wrangler/deploy/config.json` must stay in place — moving it breaks `astro preview` ("No build output found").
- Full-page screenshots: IAB fullPage=true tiles/corrupts on tall pages. Working recipe: scroll with scrollBehavior='auto' + instant scrollTo, wait document.fonts.ready, slice at 1800px, verify scrollY per slice, stitch with sharp at actual offsets. Smooth-scroll captures produced duplicated/overlapping bands that looked like site bugs.
- resolveLang: `/` alone rendered Chinese in the test browser (Accept-Language/cookie default); force `/?lang=en` for English captures.
- Wrangler CLI updated to logged-in state; Cloudflare MCP docs tools available; full mcp__cloudflare__* toolset still needs session restart to register.

## Dark-theme (vespers) fixes
- `.listen` button (Guidance / Listen / ■ Stop) in today/[step].astro kept the browser-default black text — buttons don't inherit color. Added `color: inherit`.
- `.error` in the same page hardcoded #b3261e → now `var(--terracotta)` (adapts lighter in dark).
- Swept all .astro/global.css for hardcoded low-luminance text colors: remaining ones are intentional (assistant-widget light bubbles with dark text, Google button white/#1f1f1f, bible-page fixed parchment paper). Verified in-browser with data-theme=dark probe + screenshot: Guidance/Listen now rgb(233,226,207) on charcoal.
