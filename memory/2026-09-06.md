# 2026-09-06

## Resume-after-login for the daily Lectio walk

User asked: when the app prompts for login mid-Lectio, keep all status so the
reader returns to the step they were at.

**Diagnosis:** `returnTo` already brought the browser back to the right page
(`/today/meditatio`), but an anonymous walk leaves no `daily_sessions` row, so
the first post-login page load created a fresh session at `silencio` and
`canOpenStep` bounced the reader back to `/today/silencio`. The lost "status"
was the reached step, not the URL.

**Fix:** signed `anon_progress` cookie (HMAC via SESSION_SECRET, same pattern
as `pending_draw`):

- `src/lib/anonProgress.ts` — `recordAnonProgress(cookies, day, step)` writes
  `{v:1, day, step}` signed, 7-day TTL; `takeAnonProgress(cookies, day)` reads
  + clears, day-matched, signature-checked (can't forge a skip-ahead).
- `today/[step].astro` — anonymous readers record the step on every page load
  (only silencio/lectio render for them); a freshly signed-in reader's first
  `ensureSession` seeds `reached_step` from the cookie, then the cookie is
  cleared.
- `today/index.astro` — same seeding on the entry page.
- `dailySession.ensureSession` — new optional 5th param `seedStep` used only on
  INSERT (ON CONFLICT DO NOTHING keeps existing rows untouched).

Covers email login, signup, and Google OAuth with one mechanism since the
cookie just survives the auth round-trip. Draft text needs no preservation:
anonymous readers never see a writing form (the wall is a GET redirect).

Tests: new `anonProgress.test.ts` + 2 seeding cases in `dailySession.test.ts`.
277/277 pass. `astro check`: only pre-existing error remains
(`scripts/tts/lib.mts:174` writeFileSync arity — part of the uncommitted TTS
work), plus a pre-existing ts(6133) hint on `[step].astro` returnTo (verified
it fires on HEAD too via stash round-trip).

Not committed; working tree also holds unrelated uncommitted TTS/README work.

## Committed + deployed (user asked: "commit and deploy everything")
- Fixed a real regression in the uncommitted TTS refactor before committing:
  `lib.mts` writeFileSync lost its `jobsFile` first arg when the block moved
  into the engine if/else (astro check caught it; `tts:steps` with a venv
  engine would have crashed).
- Two commits, pushed to origin/main: `17e0126` (OpenAI TTS engine + kokoro
  regeneration manifests + README + 09-05 notes) and `23795b5` (resume-after-
  login fix). `public/audio/` (1.5 GB, 3664 files) left untracked on purpose —
  previous session's pattern: manifests are the source of truth, audio ships
  via wrangler assets upload only.
- `npm run build` OK; `npx wrangler deploy` version 7d20f504. Asset store had
  everything content-hashed already ("No updated asset files to upload") — the
  new OpenAI step clips + kokoro days had reached CF earlier; only the worker
  bundle moved (4.3 MB, 61 modules).
- Live verified: new step clips 200 (en/silencio-45d02d30, zh/oratio-32f6aabe,
  both engine=openai in manifest), day clip /audio/days/zh/2026-09-06.mp3 200,
  home 200, /today 302 → silencio.

## First-party analytics + A/B testing instrumentation

Built a self-hosted analytics & A/B framework on the existing Cloudflare stack (no third-party scripts):

- `src/lib/ab.ts` — experiment registry, deterministic FNV-1a assignment from visitor id, `ab` cookie parse/serialize. Starter experiment: `signup_cta_copy` (control vs "invitation" CTA in LoginForm).
- `src/middleware.ts` (new) — mints `vid` cookie (UUID, 2y) and assigns variants on every request; exposes `Astro.locals.vid/variants`.
- `src/lib/analytics.ts` — `analytics_events` D1 table (lazy ensure, WeakSet per-db), allowlisted client events, `ingestClientEvents` (batch cap 25), `trackServerEvent` for signup_success/login_success/checkout_start.
- `src/pages/api/analytics/collect.ts` — beacon endpoint, always 204.
- `src/scripts/analytics.ts` — page tracker: auto page_view, `[data-event]` click + `[data-event-form]` submit delegation, batched sendBeacon; global `window.lectioTrack(name, props)`.
- Wired: nav Today/Pricing links, auth signup link, login overlay open, step-page audio players (passage/clip/guided), pricing checkout forms.
- Tests: ab.test.ts (12), analytics.test.ts (5) using D1Memory (added `batch()` to the helper). Full suite 294 passing, astro check 0 errors.

To read results: query `analytics_events` in D1, group by `variants`. New experiments = add to `EXPERIMENTS` in ab.ts; re-bucketing is automatic.

## Deployed analytics/A-B instrumentation (11:23)
- `npm run build` + `npx wrangler deploy` → version d05df20e-12ce-4f3d-b3cb-98e0af57c1db on 3livescapture.com.
- Verified live: homepage sets `vid` + `ab=signup_cta_copy=control` cookies; `/api/analytics/collect` returns 204.
- Note: deployed with the working tree that also had the pending scripture.ts changes.

## Roblox world connected to the backend (12:40–13:00)

Wired `src/roblox` (the Roblox place replicating the site) to the real Lectio backend. It had shipped self-contained (local 43-verse data, keyword assistant); now it's live-first:

- **Backend**: new `src/lib/roblox.ts` + `POST /api/roblox/{state,register,explore,today,assistant,library}`. Key-authed via `X-Lectio-Key` header ↔ `ROBLOX_API_KEY` secret (constant-time compare, 503 when unset, 401 wrong). Readers keyed `rb:<RobloxUserId>` in the SAME D1 tables (usage_daily, welcome_credits, chat_usage_daily) + tiny `roblox_players` registry (in-world registration = free account → 6/day + 3×3card + 1×celtic welcome credits, idempotent). Explore = real 151-verse deck + `generateInterpretation` AI reflection; today = lectionary focus resolved verse-by-verse (exported BOOK_ZH from passage.ts); assistant = site prompt/grounding, non-streaming, charge-on-first-chunk; library = full deck.
- **Roblox side**: `src_server.lua` rewritten — HttpService bridge (script attributes `LectioApiKey`/`LectioBackendUrl` override constants), per-path warn-once, 401/503 switches live mode off for the session, full offline fallback (DataStore quota + VerseData) so the place still works without HTTP. `src_client.lua` — reading panel now scrolls verse + AI reflection (+ summary on last step), position labels from SPREADS, library renders backend deck grouped by book, assistant sends on-screen reading context. Regenerated Lectio.rbxlx (python generate_lectio.py); all Lua luaparse-clean.
- **Verified**: vitest 313 pass (19 new in roblox-api.test.ts), astro check 0 errors, build OK, and a real end-to-end smoke on `astro preview` (localhost only — wrangler dev binds [::1]): 401/503 guards, explore with genuine AI reflection, quota 3→limitMsg, register→credits, divina spends credit, today=Proper 18 / Matt 5:13-20, assistant grounded reply, library 151.
- Gotchas hit: `astro dev` crashes on this machine (vite deps_ssr optimizer race, pre-existing); `npm run build` fails if dist is held open by a lingering workerd (kill orphaned workerd.exe, rm -rf dist, rebuild). Preview reads secrets from dist/server/.dev.vars baked at BUILD time — rebuild after changing .env.
- Not done: deployment (needs `wrangler secret put ROBLOX_API_KEY` + deploy), and setting the key attribute in the published place.

## Lavish UX redesign artifacts + mobile usability spec (afternoon)

- Built `.lavish/lectio-ux-redesign.html` (site-wide UX redesign proposal, styled with Lectio's own tokens): 8 code-verified findings, before/after mockups (sticky nav+footer, invitation-first homepage, inline shimmer loader), 3 directions (A Night Chapel refined = recommended / B Parchment light-first / C Silence minimal), input forms for direction + scope UX-01..08. Direction NOT yet chosen by user; poll session still open on that artifact.
- User asked to "use superpowers/brainstorm to improve mobile web usability". Skill isn't registered in ZCode session but is on disk at ~/.zcode/cli/plugins/cache/superpowers-marketplace/superpowers/6.3.0/skills/brainstorming — read SKILL.md and followed its process manually (architectural path).
- User didn't answer the AskUserQuestion (target journey) → proceeded on recommended default: daily /today practice first. Presented 3 approaches in chat + phone mockups in `.lavish/lectio-mobile-usability.html`.
- User decided via artifact forms: **Approach B (thumb-first shell + ritual mode), full scope M-01..M-07**.
- Spec written + self-reviewed + committed: `docs/superpowers/specs/2026-09-06-mobile-usability-design.md` (commit 5025a02). Key verified facts going in: logo.webp already 512x512; DailyProgress.astro exposes step state for dot restyling; no-JS fallback = keep .top-nav in DOM, hide under html.js-nav class only; A/B signup_cta_copy must not be touched (size-only auth changes).
- Next step per brainstorming skill: user reviews spec → then writing-plans skill for implementation plan. Approval gate still holds: no site code changed.
- Lavish sessions: two open (lectio-ux-redesign.html, lectio-mobile-usability.html), polls run as tracked background jobs.

## Mobile usability: spec → plan → implementation (branch `mobile-usability`, afternoon)

- User approved spec ("LGTM") → writing-plans skill → `docs/superpowers/plans/2026-09-06-mobile-usability.md` (commit 1f807d0), 8 tasks with exact code. User chose **Inline Execution** (over subagent-driven).
- Per executing-plans skill, did NOT implement on main: work is on branch `mobile-usability` (7 commits, Tasks 1–7 all landed).
- Shipped: M-03 PWA-lite (manifest + logo-192.png via scripts/make-icons.mjs + sharp devDep + script-synced single theme-color meta — spec §4.3 amended because a script can't override media-scoped metas); M-01 MobileNav.astro (≤760px thumb bar + full-screen sheet, html.js-nav progressive enhancement keeps .top-nav as no-JS fallback); M-02 ritual mode (Layout `chrome="minimal"` prop; DailyProgress `variant="dots"`; [step].astro + amen.astro full-height flex frames with sticky gold bottom action bar; writing form submit uses `form="step-form"` button in the bar; timer 'ready' now lights `.action-bar` too); M-04 safe-area/dvh on loader + gate modal; M-06 assistant z-index 9999→60, safe-area offset, `.above-action` geometry on writing steps; M-05 44px targets + 16px inputs sweep; M-07 blocking loader deleted → inline `#wait-panel` shimmer (reuses .teaser-line), reduced-motion added for .teaser-line.
- Lesson: Astro **scoped styles** don't cross components — the ritual frame styles must live in global.css (DailyProgress dots + amen page reuse them). `.daily header`/`.amen header` rules fight `.ritual-top` (both are <header>) → global `.ritual .ritual-top` (0,2,0) wins over scoped (0,1,1).
- Environment gotcha: interactive runtime is BROKEN on this machine right now — `astro dev` (vite race, pre-existing), `astro preview` (fails: "non-interactive environment... CLOUDFLARE_API_TOKEN" for the remote proxy session; works only in a TTY with wrangler login), `wrangler dev` (libuv Assertion `win/async.c:76` crash). Automated checks all green: astro check 0 errors, 313/313 vitest, build OK. The plan's manual browser matrix (Task 8 step 2) is still PENDING — needs a TTY local run or deploy to a preview. .dev.vars created locally (SESSION_SECRET only, gitignored).
- sharp added as devDependency for the one-off icon gen; safe to remove later (PNG committed).
- Next: finishing-a-development-branch skill (merge options for `mobile-usability` → main).

## Assistant chips fixed + assistant can drive the site (evening)

- Root cause of "draw a card" suggestions: CHIPS are hardcoded per-path strings in AssistantWidget.astro's client script (there since the initial commit), never aligned with the prompt's "no divination" rule; the Chinese set was already corrected but the English one wasn't. English chips now match product framing ('What is Lectio Divina?' on /, scripture-framed /library questions, 'daily readings' not 'daily draws').
- New assistant→site action system ("drive the website"):
  - `assistant.ts`: exported `ASSISTANT_ACTIONS` (ids: new_reading, today, library, history, pricing, account, signup); buildSystemPrompt appends an Actions menu telling the model to offer at most 1-2 `[label](action:id)` links on their own lines when it's the natural next step.
  - `minimark.ts` link allowlist now also passes `action:[a-z_-]+`; site-relative + action links render same-tab (no target attr), only external http(s) keep target=_blank.
  - AssistantWidget: delegated click handler on #assistant-thread intercepts `a[href^="action:"]` → ACTION_DEST map → navigation (new_reading → `/?new=1`, which clears the last-reading cookie and shows a fresh form); unknown ids ignored (stale sessionStorage threads). Action links styled as purple pill buttons.
  - Draws still require the visitor's explicit Explore click on `/` — the assistant never spends quota itself.
- Tests: minimark link test rewritten (relative/action/external + js: still rejected), new actions-menu test in assistant.test.ts. 314/314 pass, astro build OK. Not committed yet (per policy user asks first).
