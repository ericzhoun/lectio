# 2026-08-29

## inspire — Cloudflare deploy verification
- Eric deployed latest build of `inspire` (Tarot app) to Cloudflare Workers; custom domain `dramatherapy.us` live.
- Verified: 200 OK, title 「自我启发 韦特塔罗」, live bundle hash `Layout.Rb05W_EW.css` matches local `dist/`; perks feature (`WELCOME_CREDITS`: 3-card ×3, Celtic Cross ×1, `credits_Bhz10P1o.mjs`) confirmed in shipped bundle.
- Committed + pushed `b37c243` "add dramatherapy.us custom domain route and disable query string redaction" (wrangler.jsonc).

## Lessons / notes
- Git push to origin (SSH) fails from my shell: `Permission denied (publickey)`. Workaround: push via HTTPS URL — `gh` CLI is authenticated (ericzhoun, credential helper configured). User chose to keep SSH remote as-is; use HTTPS URL for pushes when needed.
- `wrangler.jsonc` has `observability.redact_query_string: false` — intentional (user informed, no concern raised).
- Commit style in this repo: lowercase short imperative sentences ("updated stripe", "update homepage wording").
