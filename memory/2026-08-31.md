# 2026-08-31

## inspire — "draw three cards, register to reveal" feature
- Eric asked for tarot.com daily-reflection-style flow in `D:\workplace\inspire`: unregistered users draw 3 cards, then get prompted to register to reveal the reading. Implemented end-to-end.
- Key pieces: `src/lib/pendingDraw.ts` (new, HMAC-signed `pending_draw` cookie, 7-day TTL), entitlements change (anonymous 3card → `{ok:true, gated:true}` instead of `spread_locked`; celtic still locked), fan UI + pending/reveal rendering in `index.astro`, auth endpoints (signup/login/google callback) redirect to `/` instead of `/account` when a pending draw exists.
- Draw is free (no OpenAI, no quota spend); reveal consumes a 3-card welcome credit via `recordDraw` and logs to `drawing_sessions`. Failure modes handled: no_credits/quota → upsell with cookie kept; LLM failure → retry gate, nothing spent.
- Reveal happens on GET `/` when registered — GET has side effects but the cookie clears after success, so no replays.
- Verification: `npm test` 91 pass (was 76), `npx astro check` clean, `npm run build` OK, and a full `wrangler dev` curl smoke test: draw → locked view + cookie; signup → 302 `/`; reveal → full reading, credits 3→2 confirmed in local D1, reading logged.
- Smoke-test gotchas learned: Astro CSRF rejects POSTs without an `Origin` header (curl needs `-H "Origin: ..."`); PowerShell `$home` is a reserved variable; extracting the `deck_order` JSON from HTML needs a lazy `\[.*?\]` regex after HtmlDecode (quotes break `[^"]+`).
- Docs: `docs/2026-08-31-draw-to-reveal.md`, README feature bullets updated. Committed locally (not pushed, not deployed — Eric handles Cloudflare deploys).

## inspire — extension: Celtic Cross + Bible flip (same day, second request)
- Eric then asked: same experience for 10 cards (draw + reveal free, reading needs registration), and Bible mode where flipping the bible reveals verses free but the question-based reading needs registration.
- Changes: entitlements now gate BOTH multi-card spreads for anonymous (`{ok:true, gated:true}`); `pendingDraw` cookie is mode-discriminated (`mode: 'tarot'|'bible'`, `cards` or `verses`, 1-10 items); `bible.ts` gained `rebuildDrawnVerses(refsEn)`; fan slots are now built client-side from a server-embedded positions map (3 or 10 slots, numbered for Celtic); Bible mode gets a "Flip open the Bible" submit-button panel (one flip draws random verses server-side) with custom loading text; pending views render cards OR verses (verse text + refs visible for free - per user's spec, only the reflection is gated); new `locked` gate variant for paid-tier spread_locked reveals.
- Verification: 95 tests pass, astro check clean, build OK, wrangler-dev E2E: Celtic 10-pick draw -> signup -> full 10-card AI reading (10 tag groups) with celtic credit 1->0; Bible 3-verse flip -> signup -> full reflections + overall + follow-ups with 3card credit 3->2. Cookies cleared both paths.
- NEW gotcha: never reuse redacted transcript text in commands - I copied `$env:O..._KEY` (with literal ellipsis) into a wrangler `--var` flag, so PowerShell passed garbage as the OpenAI key and the reveal rendered "Error generating interpretation: 401" (openai.ts catches API errors into the summary - pre-existing behavior). Fixed by expanding `$env:OPENAI_API_KEY` at runtime. Also: stale `wrangler dev` instances from earlier sessions keep serving old bundles and respawn workerd - kill the parent node (wrangler) processes first, then workerd.
- Docs updated: `docs/2026-08-31-draw-to-reveal.md`, overview HTML, README. Committed (see git log), not pushed/deployed.

## inspire — third refinement: real Bible page + button-gated registration (same day)
- Eric: after flipping the bible, verses should render ON the page with neighbouring paragraphs like a real bible; registration prompt ONLY when clicking the 解经 (interpret) button.
- Data: new `src/lib/bibleChapters.json` (273KB, 70 chapters, 1952 verses) generated by `scripts/fetch-bible-context.mjs` from api.getbible.net — WEB (en, "Yahweh" normalized to "the LORD" to match deck) + CUV 神版 (zh, cus translation). Deck text vs chapter text differ slightly by edition (甚么/什么, spacing) — context pages use chapter text consistently; readings keep deck text.
- Code: `bible.ts` gained `buildBiblePages(drawn, windowSize=6)` (parse refs incl. ranges like 13:4-5, group by book-nr:chapter, ±6-verse window, drawnIndex + isRangeStart marking, missing-chapter fallback). index.astro: reveal moved OFF GET onto POST `reveal_pending=1` (applyReveal closure); pending view setup block; bible-page template (paper styling, verse numbers, highlighted drawn verses, position tags, ellipsis); interpret-bar with one gold button — anonymous: opens modal on click; registered: submits reveal form; modal no longer auto-opens. pendingGate became object holder `pendingGateState.gate` because TS CFA narrowed a plain let in template comparisons (4 astro-check errors).
- Gotchas: reading JSON refs from bible.ts needs the en-ref regex to only match ASCII refs (zh refs are CJK); getbible v2 shape is {verses: [{verse, text}]}; bible-api.com WEB is the "Yahweh" edition so use getbible web+cus instead; PowerShell -replace chains on the declaration line failed once because earlier replaces had already rewritten the anchor (check anchors against CURRENT content).
- Verified: 100 tests, astro check clean, build OK, wrangler-dev E2E (live AI): bible flip -> paper page with 3 chapter sections / 33 verse numbers / 3 highlights / 5 ellipses; signup -> still pending (no auto-reveal) with reveal form; POST reveal -> full reflections + overall + follow-ups, credit 3->2; tarot 3-pick -> same button flow; cookies cleared. curl smoke note: reveal POST needs `-c jar` to record the cookie deletion, else follow-up GET re-renders the pending view (harness artifact, not app behavior).
