
## TTS feature added
- Added open-source TTS to lectio via Cloudflare Workers AI's MeloTTS (`@cf/myshell-ai/melotts`, MIT-licensed).
- New `AI` binding in wrangler.jsonc + `src/lib/tts.ts` + `POST /api/tts` (text+lang -> audio/mpeg).
- "▶ Listen" button on the lectio step of /today/[step]; fetches the endpoint and plays the blob; click during playback shows ■ Stop.
- Gotchas: melotts input field is `prompt` (not `text`) per workers-types; ai.run returns Uint8Array; wrap in `new Uint8Array(...)` for Response BodyInit typing.
- astro check 0 errors, 246 tests pass, build OK. Not yet deployed or verified with a live AI binding in dev.
